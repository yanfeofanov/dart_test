1. var нужен для того, чтобы мы не могли изменить переменную, dynamic - противоположность

2. при использовании const мы говорим, что при запуске программы мы 
ничего не можем изменить и это уже точное значение известное заранее, 
а когда мы используем final мы говорим, что значение будет меняться 

3. нет, нельзя 

4. 

5. по умолчанию оно имеет значение null

6. Да, можно. Оно станет типа dynamic

7. потому что final защищает переменные от случайных изменений

8. Null Safety - защита от переменных с пустыми значениями(c null)

9. потому что null не имеет вообще никаких значений, и поэтому при использовании 
переменной со значением null вылезает такая ошибка

10. достаточно добавить "?" - "String? name;"

11. когда мы используем ?? мы говорим, если значение null, то бери значение справа
пример - String? null;
         print(null ?? "гость")

12. когда ?. мы говорим, если null - верни null, но если мы ставим !.  , 
то при значении переменной null у нас будет выводится ошибка

13. Sound null safety переносит проверку на наличие null из рантайма на этап компиляции.
и при возникновении переменной с null, программа не ломается

14. когда мы ставим late, например, "late String name;" - мы говорим, 
что сейчас значения нет, но в будущем оно появится

15. опасность заключается в том, что если мы обратимся к late name раньше, 
чем объявим, то получим ошибку "LateInitializationError"

16. switch это краткая форма if-else, switch лучше использовать,
 когда у нас много условий, а при маленьком наборе условий можно использовать if-else

17. конечно!

18. оно заканчивает цикл. если его не остановить, то он будет каждый раз повторяться 

19. 

20. Использовать is в if можно и часто нужно, когда вам важно сначала проверить тип объекта,
 а затем работать с ним как с этим конкретным типом.
 Это делает код безопаснее и понятнее.
 Но помните про ограничения оператора и особенности конкретного языка программирования.

21. цикл for ,когда нам надо сделать определенное кол-во повторений,
 цикл while - когда у нас нет цели достигнуть определенное кол-во повторений

22. этот цикл позволяет перебирать элементы в массивах

23. true никак не повлияет на заканчивание цикла

24. break отвечает за остановку цикла,
 а continue - пропуск текущей итерации с переходом к следующей

25. да. чтобы выйти из всех циклов надо в последнем цикле написать break outer, либо другую похожую команду

26. цикл будет бесконечным, так как каждую итерацию i = 5

27. Map в Dart — это коллекция, которая хранит элементы в виде пар «ключ-значение».
Если говорить по простому, то ключ(key) это специальный метка для быстрого нахождения информации(value)

28. можно использовать любые типы ключей и даже null

29. в Map<String, int> строго типизированная пустая мапа,
 а var map = {} такая же пустая, но уже принимает данные любых типов

30. получить по индексу

31. когда мы ищем по индексу, лист ищет нужный нам индекс в упорядоченном массиве данных,
 а если ищем по ключу, то мапа ищет по индивидуальному ключу

32. for (var key in map.key) или for (var entry in map.entries) 

33. можно, но это приведёт к ошибке ConcurrentModificationException

34. Map.fromIterable — это конструктор в языке Dart, 
который создаёт экземпляр Map (словарь) из итерируемого объекта (например, списка)

35. класс - тело нашего проекта, например, машина, а объект - это, например, движок, колесо и тд

36. именованный конструктор отличается от обычного тем, что создавая именованный конструктор,
мы добавляем ясности в код.

37. ключевое слово this используется, чтобы сказать, что мы берем этот параметр именно из этого класса,
 например, у нас класс car,  у него параметры speed and weight, и в конструкторе с помощью this 
 мы говорим что именно эту скорость мы берем именно из этого класса

38. список инициализации - место после ":", где мы прописываем условия, которое выполняются первыми,
то есть до фигурных скобок

39. через точку (.)

40. с помощью абстрактного класса мы сокращаем код и делаем его более кратким,
то есть мы можем создать в абстрактном классе один метод,
 который в свою очередь смогут использовать обычные классы, например, лай для собак,
тем самым абстрактный класс это шаблон для наследования, 
а обычный класс это класс для унаследования и создания объектов 

41. нельзя, так как абстрактный класс является шаблоном для наследования 

42. геттеры нужны, чтобы мы могли использовать переменные/методы за пределами класса,
 а сеттеры нужны для изменения переменных/методов также за пределами класса

43. мы используем static, чтобы никто не смог воспользоваться переменной/методом за пределами класса 

44. наследование - это когда один класс использует метод другого класса,
 то есть класс-ребенок использует метод класса-родителя

45. нет

46. @override нам нужен, чтобы мы могли унаследовать метод класса-родителя

47. overriding - перегрузка, разные методы для одного класса
    overrloading - переопределение, один метод для разных классов

48. полиморфизм - когда один класс может использовать метод другого, то есть наследование.
при вызове метода animal.makeSound() вызовется метод звука(в данном случае голос для собаки)

49. инкапсуляция другими словами приватность, она достигается за счёт "_", и поле видно только в этом файле

50. Extends - наследование
    Emplements - берём от метода родителя только название
    with - через него мы используем метод из другого класса, а не из класса родителя

51. сначала родитель, потом потомок

52. когда мы пишем new MyClass (), то происходит 4 фазы:
Выделяется память
2. Заполняются поля
3. Вызываются конструкторы
4. Возвращается ссылка на объект

53.  Garbage Collector ищет объекты, которые уже никому не нужны(мёртвый код),
если через объект невозможно  добраться через ссылки - память освобождается

54. Нет, пока ссылка на объект существует, он не может удалить объект

55. Особая ссылка, которая не мешает сборщику мусора удалить объект
Используется для кэшей и служебных механизмов

56. Потому что растёт потребление памяти, чаще запускается мусорщик и падает производительность

57. Exception для самого прогера, как пометка что не так, а Error для пользователя

58. мы заворачиваем программу в эту оболочку, чтобы при возникновении ошибки программа не ломалась 

59. on ловит конкретный тип ошибки, а catch даёт доступ к самой ошибке

60. finally обеспечивает стопроцентное продолжение работы кода при возникновении ошибки.
Если добавить return, то ничего не поменяется


